import Ember from 'ember';
import layout from '../../templates/components/list-card/loading-mask';

export default Ember.Component.extend({
  classNames: ['list-card-loading-mask-component'],
  layout: layout
});
